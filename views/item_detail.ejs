<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-gray-800 text-white">
        <div class="container mx-auto px-4 py-2">
            <a href="/" class="inline-block px-4 py-2">Home</a>
        </div>
    </nav>

    <div class="container mx-auto mt-4 px-4">
        <h1 class="text-3xl font-bold mb-4"><%= title %></h1>
        <% if (item) { %>
            <div class="bg-white shadow overflow-hidden sm:rounded-lg">
                <div class="border-t border-gray-200">
                    <dl>
                        <div class="bg-white px-4 py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                            <dt class="text-sm font-medium text-gray-500">Name</dt>
                            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2"><%= item.name %></dd>
                            <dt class="text-sm font-medium text-gray-500">Description</dt>
                            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2"><%= item.description %></dd>
                            <dt class="text-sm font-medium text-gray-500">Category</dt>
                            <dd class="mt-1 text-sm text-gray-900 sm:col-span-2"><a class="underline" href="<%= category.url %>"><%= category.name %></a>
                            </dd>
                        </div>
                    </dl>
                </div>
            </div>
        <% } else { %>
            <p class="text-red-500">Item not found.</p>
        <% } %>
        <div class="w-full flex flex-col text-sm text-blue-500 items-end py-4">
            <a href="<%= item.url %>/update">Update Item</a>
            <a href="<%= item.url %>/delete">Delete Item</a>
          </div>
    </div>
    
</body>
</html>
